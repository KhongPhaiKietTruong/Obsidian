tải thêm package ipykernel vào môi trường đó bằng lệnh
```
pip install ipykernel 
```

đăng kí môi trường với jupyterlab
```
python -m ipykernel install --user --name=<tên_môi_trường> --display-name <tên_hiển_thị_trong_jupyterlab>
```