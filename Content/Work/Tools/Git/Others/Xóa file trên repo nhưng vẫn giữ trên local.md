
xóa folder:
git rm -r --cached folder

xóa file:
git rm --cached file.txt

sau đó thêm tên file/folder đó vào file .gitignore rồi add, commit và push, file hoặc folder đó sẽ không còn trên github nữa