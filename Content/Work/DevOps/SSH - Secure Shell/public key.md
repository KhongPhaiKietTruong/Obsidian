là đoạn mã được SSH tạo, nằm trên cả client và server

ta có thể xóa public key trên client và ssh vẫn hoạt động bình thường nhưng nên giữ lại để tiện kết nối thêm những server khác thay vì phải tạo lại