trong [[Inference - Test time]], [[Batch Norm]] sẽ không hoạt động giống như lúc train, khi ta truyền vào một mini-batch, thay vì sử dụng $\mu \text{ và }\sigma^2$ ([[Giá trị trung bình]] và [[Variance (Phương Sai)]]) của mini-batch hiện tại thì thay vào đó ta sử dụng [[Exponentially Weighted Average (EWA)]] của $\mu \text{ và } \sigma^2$  ở cuối được tính ra trong quá trình training và sẽ là 2 giá trị cố định để sử dụng cho batch norm ở test time 

lí do là vì nếu ta sử dụng thông số đó trong mini-batch hiện tại thì sẽ khiến [[Predicted Value (Giá Trị Dự Đoán)]] của một mẫu có thể phụ thuộc vào các mẫu khác 