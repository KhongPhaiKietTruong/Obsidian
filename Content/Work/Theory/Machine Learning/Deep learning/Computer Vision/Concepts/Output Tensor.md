là [[Tensor]] mà được ghép từ nhiều ma trận [[Feature Map]] lại 

kích thước của output tensor sẽ là $(h_k, w_k, n_f)$
với:
- $h_{k}$ là số hàng feature map
- $w_{k}$ là số cột feature map
- $n_{f}$ là số lượng [[Filter]] của lớp hiện tại