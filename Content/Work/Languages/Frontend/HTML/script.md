thẻ này dùng để kết nối Javascript vào  HTML 
```html
<script src="test.js"></script>
```
thường được đặt đằng sau thẻ [[body]] để web load được hết hmtl để hiển thị được giao diện trước khi load file js, tránh gây lag