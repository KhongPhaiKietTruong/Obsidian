thuộc tính này dùng để đặt tên cho thẻ, sau đó ta có thể dùng tên đó để định nghĩa CSS, ... 
nếu một thẻ chứa nhiều class và có sự xung đột về CSS thì nó lấy cái class được định nghĩa ở sau cùng trong file định nghĩa CSS