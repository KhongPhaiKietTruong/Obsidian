là hàm để tạo một mảng gồm các phần tử có giá trị là 0

các tham số truyền vào:
- shape: là một số nguyên nếu là mảng một chiều và là tuple nếu là mảng nhiều chiều
- dtype: mặc định là float64

```python
arr = np.zeros((2, 3))
```

