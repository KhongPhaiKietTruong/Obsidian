np.random.randn(a, b, c, ...) trả về một [[Arrays (Mảng)]] numpy với hình dạng là các tham số truyền vào mang các giá trị tuân theo [[Normal distribution (Gaussian distribution)]] với $\mu$ là 0 và $\sigma^2$ là 1 
```python 
W = np.random.rand(n_in, n_out) * np.sqrt(2 / n_in)
```