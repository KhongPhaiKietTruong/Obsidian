sudo pacman -R <package_name>

(with config file):
sudo pacman -Rn <package_name>

(with dependencies):
sudo pacman -Rs <package_name>

(with dependencies and config file):
sudo pacman -Rns <package_name>