dùng để làm mờ ảnh, 
khác với [[blur()]], gaussian blur có tính toán trọng số đối với pixel xa gần

GaussianBlur(src, ksize, sigmaX[, dst[, sigmaY[, borderType[, hint]]]]) -> dst

sigmaX: 0: tự động tính