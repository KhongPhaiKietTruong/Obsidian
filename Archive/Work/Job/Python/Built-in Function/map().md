
apply a function to all the elements in an iterable

map(function, iterable)
return a iterable (map object) 

```python
numbers = [1, 2, 3, 4]
squared = map(lambda x: x*x, numbers)
print(list(squared))
```

