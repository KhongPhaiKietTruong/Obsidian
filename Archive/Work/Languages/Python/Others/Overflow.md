khi một số **thực** quá lớn, vượt qua ngưỡng cho phép của python, nó sẽ cho ra giá trị là 'inf'

```python
a = 1e1000
print(a) #inf
```

lưu ý: hiện tượng overflow không xảy ra với số nguyên, khi một số nguyên quá lớn, nó dùng thêm RAM máy tính để lưu