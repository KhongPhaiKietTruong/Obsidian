khác với [[TCP]], UDP không phân nhỏ dữ liệu, không đánh số và cũng không bắt tay, nó chỉ thực hiện việc gửi đi dữ liệu mà không cần hỏi server có nhận được không

lợi thế của UDP là **tốc độ** (thường dùng trong game trực tuyến, ...)