với mỗi training set được tạo nên bởi [[Bagging - Sampling with replacement]], ta sẽ dùng tập đó để tạo nên một [[Decision Tree]] (khi tạo nên cây quyết định này, ta còn có thể lấy ngẫu nhiên k [[Features]] từ n đặc trưng gốc (nếu n nhỏ) hoặc $k = \sqrt{ n }$  (nếu n rất lớn) tại mỗi node) điều này còn giúp thuật toán hoạt động tốt hơn nữa), rồi từ b cây quyết định đó ta sẽ tiến hành bỏ phiếu và cho ra dự đoán cuối cùng  