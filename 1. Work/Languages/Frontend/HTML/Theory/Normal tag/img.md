thẻ này dùng để hiển thị hình ảnh 
```html
<img src="cat.webp" alt="a cat is lying and looking at the camera">
```

thuộc tính alt là dòng chữ sẽ hiển thị nếu ảnh vì một lí do nào đó mà không thể hiển thị 
thuộc tính này cũng giúp cho SEO hoạt động tốt vì nó mô tả bức ảnh 