cũng giá giống return nhưng thay vì trả về giá trị và dừng hàm thì yield trả về giá trị và chỉ dừng tạm thời, khi [[Generator]] của hàm đó được gọi bằng next() thì nó sẽ chạy tiếp tới yield tiếp theo (cũng khá giống với checkpoint) 

