[[Method (Phương thức)]] này dùng để đưa mode vào trạng thái training (cho phép [[Dropout Layer]] và [[Batch Norm Layer]] chạy) 

lưu ý là chỉ cho phép chạy thôi chứ nếu [[Tạo kiến trúc model - mạng neural]] ta không thêm nó vào thì mạng sẽ không chạy nó 