getattr(obj, name)
hàm này dùng để lấy ra [[Attribute]] của một đối tượng nếu name là tên của thuộc tính, còn nếu name là tên của một [[Method]] thì hàm này trả về một [[1. Work/Theory/OOP/Theory/Class]] tên là "method" 

