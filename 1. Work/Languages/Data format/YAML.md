Đây là các file mang dữ liệu ở dạng văn bản dễ đọc như các thông số cấu hình
là định dạng siêu tập con của [[JSON]]
```yaml
server:
  host: 0.0.0.0
  port: 8080
  ssl:
    enabled: true
  routes:
    - /api
    - /auth
```
