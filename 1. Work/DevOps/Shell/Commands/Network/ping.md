kiểm tra khả năng kết nối của máy tính với một máy chủ khác bằng cách gửi các [[Network Packet]] để hỏi xem máy chủ đó có thể kết nối không

ping <host_name>
```
ping google 
```
các options:
- -c: count (số gói sẽ gửi)
- -<4/6>: chỉ định giao thức IP sẽ sử dụng 